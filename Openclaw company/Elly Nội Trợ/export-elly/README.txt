Elly export package

Included:
- Persona/context files: AGENTS.md, SOUL.md, USER.md, IDENTITY.md, TOOLS.md, HEARTBEAT.md
- Memory files: MEMORY.md and memory/*.md present in workspace
- Installed skill list relevant to household work

Excluded by default for safety:
- Bot tokens, channel credentials, device identity, and OpenClaw global config outside the workspace
- Large unrelated project files in the workspace

If you want a second export that also includes global OpenClaw config/credentials, ask explicitly.
